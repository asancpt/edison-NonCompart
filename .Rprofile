.libPaths('./lib')
print(.libPaths())

# set a CRAN mirror
local({r <- getOption("repos")
      r["CRAN"] <- "https://cran.rstudio.com"
      options(repos=r)})

#Sys.setenv(RSTUDIO_PANDOC="/SYSTEM/pandoc/1.17.0.3/bin")
Sys.setenv(RSTUDIO_PANDOC="/home/shan/pandoc")
